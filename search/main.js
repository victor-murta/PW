

function BuscarEspecializacao(buscar, selecionar){
    const valorBuscado = buscar.value + "";
    const valorFinal = valorBuscado.toLowerCase().split('');

    valorFinal.forEach((elemento) => {
        console.log(elemento);
    })

    
}

